<h1>List of related verses</h1>
<ol>
  <% @question.references.each do|r| %>
 <br><br><b> <%= r.issue %></b><br><br>
   <h3><%=  parse_tags(r.from,r.to,r) %></h3>
       <%= truncate(@verses[r.id.to_s].translate_to("eng")[0].text, :length => 300, :omission => '...') %><br><br>
       <% end %>
</ol>

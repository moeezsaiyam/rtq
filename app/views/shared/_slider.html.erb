<div id="carousel">
<ul>
  <% @images.each do |image| %>
    <li>
      <%= link_to (image_tag(image.photo.url(:medium)), url_for(image.url), :target => "_blank") %>
    </li>
  <% end %>
</ul>
</div>
